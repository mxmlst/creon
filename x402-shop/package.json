{
  "name": "@creon/x402-shop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -w tsc -p tsconfig.json",
    "lint": "pnpm -w eslint .",
    "test": "pnpm -w vitest run --passWithNoTests",
    "typecheck": "pnpm -w tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {}
}
